{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "A boilerplate for creating websites with handlebars based templates.",
  "author": "Milan Bargiel",
  "license": "MIT",
  "scripts": {
    "handlebars": "hbs 'dev/templates/[!_]*.hbs' --partial 'dev/templates/_*.hbs' --output 'dist/'",
    "image": "imagemin 'dev/images/*.{png,jpg,gif,svg,ico}' --out-dir=dist/images",
    "scss": "node-sass 'dev/scss' --output 'dist/'",
    "watch:templates": "onchange 'dev/templates/**/*.hbs' -- npm run handlebars",
    "serve": "browser-sync start --server 'dist' --files 'dist'",
    "start": "run-p serve watch:templates"
  },
  "scriptsComments": {
    "handlebars": "render templates with partials (filename starts with underscore) to dist folder",
    "image": "optimize images and output in dist folder",
    "watch:templates": "when a handlebars file change run handlebars to re-render",
    "serve": "start a local dev server and reload browser on filechange in dist folder",
    "start": "run serve and watch tasks in parallel"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "hbs-cli": "^1.3.0",
    "imagemin-cli": "^5.1.0",
    "node-sass": "^4.13.1",
    "npm-run-all": "^4.1.5",
    "onchange": "^6.1.0"
  },
  "browserslist": [
    "Chrome >= 35",
    "Firefox >= 30",
    "Edge >= 12",
    "Explorer >= 9",
    "iOS >= 7",
    "Safari >= 8",
    "Android >= 4",
    "Opera >= 12"
  ]
}
